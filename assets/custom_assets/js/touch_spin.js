$('.apply_touch_spin').TouchSpin({
    buttondown_class: 'btn btn-secondary',
    buttonup_class: 'btn btn-secondary',

    min: 0,
    max: 999999999,
    step: 1,
    decimals: 0,
    boostat: 1,
    maxboostedstep: 1,
});